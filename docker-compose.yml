services:
  db:
    image: postgres:18
    restart: unless-stopped
    environment:
      TZ: America/Sao_Paulo
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - fiber-splice-locator-network

  fiber-splice-locator:
    build:
      context: .
      dockerfile: Dockerfile
    image: felipeschwartz/fiber-splice-locator:latest
    restart: unless-stopped
    environment:
      TZ: America/Sao_Paulo
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/${POSTGRES_DB}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - fiber-splice-locator-network
    volumes:
      - ./service-order-photos:/var/lib/my-application/service-order-photos

  fiber-splice-locator-web:
    image: felipeschwartz/fiber-splice-locator-web:latest
    restart: unless-stopped
    environment:
      TZ: America/Sao_Paulo
    ports:
      - "8081:80"
    depends_on:
      - fiber-splice-locator
    networks:
      - fiber-splice-locator-network

  portainer:
    image: portainer/portainer-ce:lts
    restart: unless-stopped
    ports:
      - "127.0.0.1:9443:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data

volumes:
  postgres_data:
  portainer_data:
  service-order-photos:

networks:
  fiber-splice-locator-network:
    driver: bridge